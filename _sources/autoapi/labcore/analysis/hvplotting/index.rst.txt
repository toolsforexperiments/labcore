labcore.analysis.hvplotting
===========================

.. py:module:: labcore.analysis.hvplotting

.. autoapi-nested-parse::

   HoloViews-based plotting for labstack.
   Contains a set of classes and functions that can be used to plot labstack-style data.

   Important Classes:
       - Nodes:
           - Node: a base class for all nodes. Nodes are the basic blocks we use for
               processing data. They can be chained together to form a pipeline.
           - LoaderNode: a node that loads and preprocesses data.
           - ReduxNode: a node that reduces data dimensionality (e.g. by averaging).
           - ValuePlot: plots data values.
           - ComplexHist: plots histograms of complex data ('IQ readout histograms').

       - Widgets:
           - XYSelect: a widget for selecting x and y axes.


   #TODO:

       * When fitting real and imaginary data, the color of the dot and line between data crosses. For example if the imaginary line is red with dots red, after adding a fit to the real data, the dots of imaginary will remain red, but the lines will be orange and the dots of the fit will orange but the lines red
       * Program refuses to open if it cannot find the config file.
       * Program refuses to open if it cannot find data properly formatted. This might be ok but a nice error message should appear.



Attributes
----------

.. autoapisummary::

   labcore.analysis.hvplotting.logger
   labcore.analysis.hvplotting.Data
   labcore.analysis.hvplotting.DataDisplay


Classes
-------

.. autoapisummary::

   labcore.analysis.hvplotting.Node
   labcore.analysis.hvplotting.ReduxNode
   labcore.analysis.hvplotting.XYSelect
   labcore.analysis.hvplotting.PlotNode
   labcore.analysis.hvplotting.ValuePlot
   labcore.analysis.hvplotting.ComplexHist
   labcore.analysis.hvplotting.MagnitudePhasePlot


Functions
---------

.. autoapisummary::

   labcore.analysis.hvplotting.plot_df_as_2d
   labcore.analysis.hvplotting.plot_xr_as_2d
   labcore.analysis.hvplotting.labeled_widget
   labcore.analysis.hvplotting.plot_data


Module Contents
---------------

.. py:data:: logger

.. py:data:: Data

   Type alias for valid data. Can be either a pandas DataFrame or an xarray Dataset.

.. py:data:: DataDisplay

   Type alias for displaying raw data.

.. py:class:: Node(path=None, data_in: Optional[Data] = None, *args: Any, **kwargs: Any)

   Bases: :py:obj:`panel.viewable.Viewer`


   Node base class.

   A simple wrapper class that we use to standardize the way we process data.
   Aim: whenever input data is set/updated, ``Node.process`` is called (in the
   base class it simply sets output equal to input).
   User-defined nodes may watch ``data_in`` and ``data_out`` to update UI or
   anything else.
   Pipelines are formed by appending nodes to each other using ``Node.append``.

   Nodes with graphs must implement a ``get_plot()`` function that returns
   a holoviews graph object in order for that graph to be able to be saved
   as either an html or png file.

   Params
   ------
   data_in
       input data. Must be either a pandas DataFrame or an xarray Dataset.
   data_out
       processed output data. Must be either a pandas DataFrame or an xarray Dataset.
   units_in
       units of input data. Must be a dictionary with keys corresponding to dimensions,
       and values corresponding to units.
   units_out
       units of output data. Must be a dictionary with keys corresponding to dimensions,
       and values corresponding to units.
   meta_in
       any input metadata. Arbitrary keys/value.
   meta_out
       any output metadata. Arbitrary keys/value.

   Constructor for ``Node``.

   :param data_in: Optional input data.
   :param \*args: passed to ``pn.viewable.Viewer``.
   :param \*\*kwargs: passed to ``pn.viewable.Viewer``.


   .. py:attribute:: data_in


   .. py:attribute:: data_out


   .. py:attribute:: units_in


   .. py:attribute:: units_out


   .. py:attribute:: meta_in


   .. py:attribute:: meta_out


   .. py:attribute:: layout


   .. py:attribute:: graph_types


   .. py:attribute:: plot_type_select


   .. py:attribute:: file_path
      :value: None



   .. py:method:: render_data(data: Optional[Data]) -> DataDisplay
      :staticmethod:


      Shows data as renderable object.

      :raises NotImplementedError: if data is not a pandas DataFrame or an xarray Dataset.



   .. py:method:: data_dims(data: Optional[Data]) -> tuple[list[str], list[str]]
      :staticmethod:


      Returns the dimensions of the data.

      Format: (independents, dependents); both as lists of strings.

      :raises NotImplementedError: if data is not a pandas DataFrame or an xarray Dataset.



   .. py:method:: mean(data: Data, *dims: str) -> Data
      :staticmethod:


      Takes the mean of data along the given dimensions.

      :param data: input data.
      :param \*dims: dimensions to take the mean along

      :rtype: data after taking the mean

      :raises NotImplementedError: if data is not a pandas DataFrame or an xarray Dataset.



   .. py:method:: split_complex(data: Data) -> Data
      :staticmethod:


      Split complex dependents into real and imaginary parts.

      TODO: should update units as well

      :param data: input data.

      :rtype: data with complex dependents split into real and imaginary parts.

      :raises NotImplementedError: if data is not a pandas DataFrame or an xarray Dataset.



   .. py:method:: complex_dependents(data: Optional[Data]) -> dict[str, dict[str, str]]
      :staticmethod:


      Returns a dictionary of complex dependents and their real/imaginary parts.

      Requires that complex data has already been split.

      :param data: input data.

      :returns: "`dependent`": {"real": "`dependent_Re`", "imag": "`dependent_Im`"}}
                `dependent_Re` and `dependent_Im` are the dimensions actually present
                in the data.
      :rtype: dictionary of the form



   .. py:method:: dim_label(dim: str, which: str = 'out') -> str

      Generate dimension label for use in plots.

      :param dim: dimension name.
      :param which: Either "in" or "out", depending on whether we want the input or
                    output data of the Node. Default is "out".

      :rtype: dimension label, including units if available.



   .. py:method:: dim_labels(which: str = 'out') -> dict[str, str]

      Generate dimension labels for use in plots.

      Generates all dimension labels for the data.
      See ``Node.dim_label`` for more information.



   .. py:method:: update(*events: param.parameterized.Event) -> None

      Update the node using external events.

      If event contains ``data_out``, ``units_out``, or ``meta_out``,
      will set them as ``data_in``, ``units_in``, or ``meta_in`` respectively.



   .. py:method:: process() -> None

      Process data.

      By default, simply sets ``data_out`` equal to ``data_in``.

      Can/Should be overridden by subclasses to do more complicated things.



   .. py:method:: data_in_view() -> DataDisplay

      Updating view of input data (as table; as provided by the data type).

      Updates on change of ``data_in``.



   .. py:method:: data_out_view() -> DataDisplay

      Updating view of output data (as table; as provided by the data type).

      Updates on change of ``data_out``.



   .. py:method:: plot() -> panel.viewable.Viewable

      A reactive panel object that allows selecting a plot type, and shows the plot.

      Updates on change of ``data_out``.



   .. py:method:: plot_obj() -> Optional[Node]

      The actual plot object.

      Updates on change of ``data_out`` or the selection of the plot value.

      :rtype: A dedicated plotting node.



   .. py:method:: append(other: Node) -> None


   .. py:method:: detach(other: Node) -> None


   .. py:method:: fit_obj()


.. py:class:: ReduxNode(*args, **kwargs)

   Bases: :py:obj:`Node`


   Node base class.

   A simple wrapper class that we use to standardize the way we process data.
   Aim: whenever input data is set/updated, ``Node.process`` is called (in the
   base class it simply sets output equal to input).
   User-defined nodes may watch ``data_in`` and ``data_out`` to update UI or
   anything else.
   Pipelines are formed by appending nodes to each other using ``Node.append``.

   Nodes with graphs must implement a ``get_plot()`` function that returns
   a holoviews graph object in order for that graph to be able to be saved
   as either an html or png file.

   Params
   ------
   data_in
       input data. Must be either a pandas DataFrame or an xarray Dataset.
   data_out
       processed output data. Must be either a pandas DataFrame or an xarray Dataset.
   units_in
       units of input data. Must be a dictionary with keys corresponding to dimensions,
       and values corresponding to units.
   units_out
       units of output data. Must be a dictionary with keys corresponding to dimensions,
       and values corresponding to units.
   meta_in
       any input metadata. Arbitrary keys/value.
   meta_out
       any output metadata. Arbitrary keys/value.

   Constructor for ``Node``.

   :param data_in: Optional input data.
   :param \*args: passed to ``pn.viewable.Viewer``.
   :param \*\*kwargs: passed to ``pn.viewable.Viewer``.


   .. py:attribute:: OPTS
      :value: ['None', 'Mean']



   .. py:attribute:: coordinates


   .. py:attribute:: operations


   .. py:attribute:: layout


   .. py:method:: on_input_update()


   .. py:method:: on_operations_change()


   .. py:method:: on_widget_change(*events)


   .. py:method:: process()

      Process data.

      By default, simply sets ``data_out`` equal to ``data_in``.

      Can/Should be overridden by subclasses to do more complicated things.



.. py:class:: XYSelect

   Bases: :py:obj:`panel.viewable.Viewer`


   .. py:attribute:: value


   .. py:attribute:: options


   .. py:method:: on_option_change()


.. py:class:: PlotNode(path=None, *args, **kwargs)

   Bases: :py:obj:`Node`


   PlotNode, Node subclass

   A superclass of all nodes that make plots of some sort. Mostly
   deals with define/creating fits for whatever graph subnode is instantiated.

   Constructor for ``Node``.

   :param data_in: Optional input data.
   :param \*args: passed to ``pn.viewable.Viewer``.
   :param \*\*kwargs: passed to ``pn.viewable.Viewer``.


   .. py:attribute:: refresh_graph


   .. py:attribute:: FITS
      :value: None



   .. py:method:: load_fits_from_config()
      :staticmethod:



   .. py:attribute:: path
      :value: None



   .. py:attribute:: fit_dict
      :type:  dict


   .. py:attribute:: fit_result
      :value: None



   .. py:attribute:: fit_inputs
      :value: None



   .. py:attribute:: save_fit_button
      :value: None



   .. py:attribute:: reguess_fit_button
      :value: None



   .. py:attribute:: model_fit_button
      :value: None



   .. py:attribute:: refit_button
      :value: None



   .. py:attribute:: fit_box
      :value: None



   .. py:attribute:: fit_button


   .. py:attribute:: select_fit_axis


   .. py:attribute:: fit_layout


   .. py:method:: get_fit_panel()


   .. py:method:: process()

      Make a copy of the data so that changes (added fits) don't carry
      to other graphs/other analysis.
      Add saved arguments for all fits/axes.



   .. py:method:: plot_panel()

      Creates and returns a panel with the class's plot

      Should be overridden by subclasses to return the desired plot.



   .. py:method:: get_plot()

      Returns the plot as a holoviews object

      Used for saving the plot as html or png.

      Can/Should be overridden by subclasses to return the appropriate object.



   .. py:method:: fit_axis_options() -> list

      Returns a list of the different axes you can
      make a fit for in this node.

      Should be overridden by subclasses to return the appropriate object.



   .. py:method:: set_fit_box(*events: param.parameterized.Event, fitted: bool = None)


   .. py:method:: set_fit_box_helper(new_box: bool, fit_func_name: str, fitted: bool = False)

      Removes and/or creates a fit box. If new_box == True this
      will (re)create the fit box.



   .. py:method:: remove_fit_box()


   .. py:method:: add_fit_box(selected=None, fitted=False)

      Create a widget box for creating a fit.



   .. py:method:: save_fit(*events: param.parameterized.Event)

      Saves only the currently selected fit axis to the json file



   .. py:method:: reguess_fit(event)

      Resets the current args to the results of the Fit.guess() function for the
      current fit class.



   .. py:method:: model_fit(*events: param.parameterized.Event)

      Models the fit starting with the arguments already created



   .. py:method:: get_arguments()

      Gets argument values for the currently selected fit and
      fit axis. Pulls data from the json if one exists, otherwise
      runs fit.guess and takes those values.



   .. py:method:: get_ansatz()


   .. py:method:: update_fit_args(event)

      Updates the temporary saved value for all of the fit's starting arguments.

      Called whenever a float input's value is changed, when the fitbox is
      created, or when the fit_axis changes.



   .. py:method:: update_dataset_by_fit_and_axis(fit_class: labcore.analysis.fit.Fit, model_args: dict[str, float], model_axis_name: str, saved: bool = False)

      Updates the data for fit in the self.data_out dataset based
      on the given arguments.



   .. py:method:: update_dataset_by_data(fit_data: numpy.ndarray, name: str)


   .. py:method:: get_data_fit_names(axis_name, omit_axes=None)


   .. py:method:: get_values(axis: str)

      Gets a dictionary of values from the result of the FitResult's params_to_dict() function.



   .. py:method:: indep_dims() -> int


.. py:class:: ValuePlot(*args, **kwargs)

   Bases: :py:obj:`PlotNode`


   PlotNode, Node subclass

   A superclass of all nodes that make plots of some sort. Mostly
   deals with define/creating fits for whatever graph subnode is instantiated.

   Constructor for ``Node``.

   :param data_in: Optional input data.
   :param \*args: passed to ``pn.viewable.Viewer``.
   :param \*\*kwargs: passed to ``pn.viewable.Viewer``.


   .. py:attribute:: xy_select


   .. py:attribute:: layout


   .. py:method:: plot_options_panel()


   .. py:method:: plot_panel()

      Creates and returns a panel with the class's plot

      Should be overridden by subclasses to return the desired plot.



   .. py:method:: fit_axis_options()

      Returns a list of the different axes you can
      make a fit for in this node.

      Should be overridden by subclasses to return the appropriate object.



.. py:class:: ComplexHist(*args, **kwargs)

   Bases: :py:obj:`PlotNode`


   PlotNode, Node subclass

   A superclass of all nodes that make plots of some sort. Mostly
   deals with define/creating fits for whatever graph subnode is instantiated.

   Constructor for ``Node``.

   :param data_in: Optional input data.
   :param \*args: passed to ``pn.viewable.Viewer``.
   :param \*\*kwargs: passed to ``pn.viewable.Viewer``.


   .. py:attribute:: gb_select


   .. py:attribute:: layout


   .. py:method:: plot_panel()

      Creates and returns a panel with the class's plot

      Should be overridden by subclasses to return the desired plot.



   .. py:method:: get_plot()

      Returns the plot as a holoviews object

      Used for saving the plot as html or png.

      Can/Should be overridden by subclasses to return the appropriate object.



   .. py:method:: fit_axis_options()

      Returns a list of the different axes you can
      make a fit for in this node.

      Should be overridden by subclasses to return the appropriate object.



.. py:class:: MagnitudePhasePlot(*args, **kwargs)

   Bases: :py:obj:`PlotNode`


   PlotNode, Node subclass

   A superclass of all nodes that make plots of some sort. Mostly
   deals with define/creating fits for whatever graph subnode is instantiated.

   Constructor for ``Node``.

   :param data_in: Optional input data.
   :param \*args: passed to ``pn.viewable.Viewer``.
   :param \*\*kwargs: passed to ``pn.viewable.Viewer``.


   .. py:attribute:: xy_select


   .. py:attribute:: layout


   .. py:method:: process()

      Make a copy of the data so that changes (added fits) don't carry
      to other graphs/other analysis.
      Add saved arguments for all fits/axes.



   .. py:method:: plot_options_panel()


   .. py:method:: plot_panel()

      Creates and returns a panel with the class's plot

      Should be overridden by subclasses to return the desired plot.



   .. py:method:: fit_axis_options()

      Returns a list of the different axes you can
      make a fit for in this node.

      Should be overridden by subclasses to return the appropriate object.



   .. py:method:: get_data_fit_names(axis_name)


.. py:function:: plot_df_as_2d(df, x, y, dim_labels=None, graph_axes=None)

.. py:function:: plot_xr_as_2d(ds, x, y, dim_labels=None, graph_axes=None)

.. py:function:: labeled_widget(w, lbl=None)

.. py:function:: plot_data(data: Union[pandas.DataFrame, xarray.Dataset]) -> panel.viewable.Viewable

